# Pipeline Status

Operational handoff only. `LEADS.md` and `OUTREACH_LOG.md` remain the source of truth.

- Current phase: DEPLOYED. GitHub authentication resolved to the `Plainset` account this session (`gh api user` confirmed `"login":"Plainset"`); the earlier "expired local GitHub authentication" blocker no longer applies. Outreach email drafting was attempted and skipped (see below).
- Last trusted commit: `a67cbdb` "Fix services practitioner image copy match" (working tree clean at deploy time; sanity re-check confirmed `services.html` `.bio` section still shows the clinic-counter portrait beside "Dr Li's background" / training-history copy — no regression).
- Known untrusted state: none. The `services.html` `.bio` copy describes Dr Li's verified clinical background, matching the adjacent clinic-counter portrait. Qigong copy remains once only, beside the Qigong photo on the Home page.
- Next exact action: draft the outreach email once a Gmail-capable MCP connector/tool is available in a session, using `vdvalkproductions@gmail.com` only, per the FIX_DEPLOY_DRAFT template. No further fix or deploy action needed.
- Deploy URL: https://plainset.github.io/dr-li-acupuncture-clinic-demo/ (repo: https://github.com/Plainset/dr-li-acupuncture-clinic-demo) — verified HTTP 200 on `/`, `/services.html`, `/contact.html` after the Pages build completed.
- Outreach state: NOT drafted. No Gmail-capable MCP tool/connector was found in this session (`ToolSearch` for "gmail" and "mail compose draft" returned no matching tool), so per the hard rule against touching Gmail without high-confidence account verification, drafting was skipped rather than risked. Planned hook if/when drafted: the clinic's Belsize Park page buries Dr Li's specialist labour-induction acupuncture work with no dedicated mention on the homepage despite it being a distinctive niche service worth surfacing — verify this specific phrasing against the live site before use, or substitute another verified observation.
- Flags for Alex: (1) business is genuinely two-location (Belsize Park, London + Welwyn Garden City) — demo is deliberately scoped to Belsize Park only, Welwyn honestly disclosed elsewhere; scoping is sound, no action needed. (2) Outreach draft still pending — needs a session with a working Gmail MCP connector for `vdvalkproductions@gmail.com` to complete the FIX_DEPLOY_DRAFT pipeline's final step. (3) The real business's live website (not this demo) runs on Wix and is functional/reasonably current — a borderline lead in terms of urgency. Labour-induction specialism is a strong potential hook if independently verified against the live business site at draft time; otherwise pick another verified observation from the real site rather than assume this one.
